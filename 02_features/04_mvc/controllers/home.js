const template = require('./../views/template-main');
const test_data = require('../model/test-data');

exports.get = (req,res) => {
    const teamList = test_data.teamList;
    var strTeam = "",i=0;
}